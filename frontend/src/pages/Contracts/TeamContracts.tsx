import React from 'react';
import { Box, Typography } from '@mui/material';

const TeamContracts: React.FC = () => {
  return (
    <Box>
      <Typography variant="h5" sx={{ mb: 3 }}>Контракты моей команды</Typography>
      <Typography color="text.secondary">Раздел в разработке</Typography>
    </Box>
  );
};

export default TeamContracts;
