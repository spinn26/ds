# Скопируйте этот файл в .env.local для разработки, либо в .env.production
# для production-сборки. Файлы .env.local / .env.*.local в git не попадают.
VITE_API_BASE=https://dev.dsconsult.ru/api/v1
VITE_SOCKET_URL=https://dev.dsconsult.ru
VITE_APP_NAME=DS Partner
