node_modules/
dist/
dist-ssr/
*.local
.env
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json
.idea/
*.swp

# Build artifacts
.cache/
.parcel-cache/
*.tsbuildinfo

# Capacitor native projects: добавляются позже при `npx cap add android/ios`.
# Сами папки коммитим (там есть конфиги signing/permissions),
# а зависимости — нет.
ios/App/Pods/
ios/App/build/
ios/DerivedData/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/

# Signing — секреты, NEVER коммить
android/app/keystore.properties
android/app/*.keystore
android/app/*.jks

# OS
.DS_Store
Thumbs.db
